{
  "name": "underthehazeltree",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Under the Hazel Tree \u2014 a place for poems, stories and quiet reflections.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev -c dist/server/wrangler.json",
    "check": "astro check",
    "art": "node scripts/art.mjs",
    "deploy": "astro build && wrangler deploy -c dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.1",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/cormorant-garamond": "^5.3.0",
    "@fontsource-variable/eb-garamond": "^5.3.0",
    "astro": "^7.2.2",
    "wrangler": "^4.123.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
